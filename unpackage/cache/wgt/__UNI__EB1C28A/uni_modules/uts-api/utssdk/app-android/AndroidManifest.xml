<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application>
    <!-- 注册自定义Activity，包名需与UTS插件实际包名一致 -->
    <activity
      android:name="uts.sdk.modules.utsApi.HelloActivity"
      android:theme="@android:style/Theme.Light.NoTitleBar"
      android:exported="false"
      android:label="你好页面" />
    <!-- ...existing code... -->
  </application>
</manifest>